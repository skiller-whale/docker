- You should not use `apt-get` and instead use an appropriate base image.
- You need different base images for the build and nginx. There is a node image available (e.g. `node:20.8.1-alpine`).
- You need to modify the COPY command to copy from the build stage.
