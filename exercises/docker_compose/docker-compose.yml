# docker-compose.yml
version: '3.7' # This version refers to the version of the docker-compose file specification.
services:
  frontend_service:
    build:
      context: frontend
    ports:
      - 1001:80
